import ArchiveValidator from './interfaces/archive.validator.interface';

export default class ArchiveValidatorImpl implements ArchiveValidator {
  validateCreateArchive(createOptions: unknown) {
    return [];
  }
}
